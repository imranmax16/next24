import{describe,it,expect}from"vitest";import{canonicalizeUrl,fingerprint,similarity,normalize,publicationDecision}from"../packages/news-core/src/pipeline";import{weightedLength,validatePost,XPublisher}from"../packages/x-publisher/src/index";
const source={id:"afad",name:"AFAD",enabled:true,sourceType:"official" as const,acquisitionMethod:"fixture" as const,priority:100,trustScore:99,language:"tr",categories:["disaster"],pollIntervalMs:5000,requiresLicense:false,credentialsConfigured:true};
const raw={externalId:"1",title:"SON DAKİKA: Malatya'da 4,7 büyüklüğünde deprem",url:"https://x.test/a/?utm_source=rss#top",publishedAt:"2026-08-14T11:00:00Z",language:"tr",raw:{}};
describe("deterministic pipeline",()=>{it("canonicalizes tracking URLs",()=>expect(canonicalizeUrl(raw.url)).toBe("https://x.test/a"));it("creates stable fingerprints",()=>expect(fingerprint(raw.title)).toBe(fingerprint(raw.title)));it("detects similar coverage",()=>expect(similarity("Malatya'da 4,7 büyüklüğünde deprem","Malatya 4,7 deprem ile sarsıldı")).toBeGreaterThan(.3));it("scores and fast-tracks official breaking news",()=>{const i=normalize(raw,source,new Date("2026-08-14T11:00:02Z"));expect(i.breakingScore).toBeGreaterThanOrEqual(80);expect(publicationDecision(i,1,"medium")).toBe("FAST")});it("uses X weighted length",()=>{expect(weightedLength("Türkçe ✅ https://example.com/long/path")).toBeLessThan(50);expect(validatePost("x".repeat(281)).valid).toBe(false)});it("publishes idempotently in dry run",async()=>{const p=new XPublisher(true);expect(await p.publish("Test","key")).toEqual(await p.publish("Test","key"))})});
