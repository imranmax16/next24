import fs from"node:fs/promises";import pg from"pg";const url=process.env.DATABASE_URL;if(!url)throw new Error("DATABASE_URL required");const client=new pg.Client({connectionString:url,ssl:/supabase\.co/i.test(url)?{rejectUnauthorized:false}:undefined});await client.connect();try{await client.query(await fs.readFile(new URL("./migrations/0004_prediction_analysis.sql",import.meta.url),"utf8"));console.log("applied 0004_prediction_analysis.sql")}finally{await client.end()}
